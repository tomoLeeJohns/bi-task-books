{
  "name": "bi-task-books",
  "version": "1.0.0",
  "description": "bi test task",
  "main": "index.js",
  "scripts": {
    "start": "npm run install-all && concurrently \"npm run server\" \"npm run client\"",
    "start-dev": "npm run install-all && concurrently \"npm run server-dev\" \"npm run client-dev\"",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "server": "cd server && npm run build",
    "server-dev": "cd server && npm run dev",
    "client": "cd client && npm run build",
    "client-dev": "cd client && npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
